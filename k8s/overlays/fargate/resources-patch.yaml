apiVersion: apps/v1
kind: Deployment
metadata:
  name: webapp-fms
  namespace: webapp-fms
spec:
  template:
    spec:
      containers:
        - name: webapp-fms
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 512Mi
